
- fsync before acking replication stream - though not sure this is that important considering the durability of the shape log queue files. havin the wal buffer more durable than them isn't worth much
- make the seq ids of the shape log queues based on the wal & offset as per lmdb. that way if we get dupes from the replication stream they will have the same id and the durable stream will reject them (unlike the current monotonic ids which will always produce a valid seq)
- don't need 3 queues for the shapes any more. the 3 queue system was to work around lmdb compaction. with the simpler file queue just need two. the snapshotter can write directly to the output queue and we only need to copy across entries in the replication buffer. tbh not sure what's going on with the 3 queues right now. don't think the snapshotter is writing to a separate queue anyway.
- 3 ms time in shape queue? seems high
