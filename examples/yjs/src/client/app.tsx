import React from "react"
import ReactDOM from "react-dom/client"
import ElectricEditor from "./components/electric-editor"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ElectricEditor />
  </React.StrictMode>
)
