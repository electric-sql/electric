---
"@electric-ax/agents-runtime": patch
---

Preserve and surface detailed run failure information from the Pi adapter so failed runs can render actionable error details instead of a generic failure message.
