{
  "name": "electric-monorepo",
  "private": true,
  "description": "",
  "dependencies": {
    "@changesets/cli": "^2.26.2"
  },
  "scripts": {
    "bump": "pnpm exec changeset version && pnpm install && pnpm run -r --if-present post-version-bump"
  }
}
